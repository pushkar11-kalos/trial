# URL of the MetraCheck FastAPI backend, reachable from the BROWSER (not
# just from the frontend container) -- e.g. http://localhost:8000 for local
# dev, or the backend service's published port in docker-compose.
NEXT_PUBLIC_API_URL=http://localhost:8000
