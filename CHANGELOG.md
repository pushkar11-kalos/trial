# CHANGELOG — MetraCheck build sessions

Newest first. Each entry is one working session in this conversation.

## Session 4 — 2026-08-30
Instruction: continue the remaining frontend pages, finish Docker config,
run end-to-end verification, produce the final completion summary, update
TODO.md/CHANGELOG.md. Also answered a direct question about remaining
effort by pointing out most of the requested "remaining" pages (Repository,
Officer Review, Reports, Rule Management) were already built in Session 3.

- Wrote the Audit Trail page (last remaining frontend page).
- **First-ever compile of the frontend**: `npm install` (145 packages,
  clean), `npx tsc --noEmit` (zero errors, first attempt, all 40 files),
  `npm run build` (succeeded, first attempt, 9 routes) — the only build
  warning was Next.js failing to fetch+minify the Google Fonts CSS at build
  time because this sandbox's network egress allowlist doesn't include
  `fonts.googleapis.com`; purely a sandbox artifact, doesn't affect the
  shipped app (fonts still load fine at runtime in a normal environment).
- Wrote `backend/Dockerfile`, `backend/entrypoint.sh`, `frontend/Dockerfile`
  (3-stage, with `NEXT_PUBLIC_API_URL` correctly wired as a build ARG, not
  just a runtime env var), and root `docker-compose.yml`. No Docker daemon
  was available in this sandbox to actually build the images — validated
  structurally instead (YAML parses, healthcheck-gated `depends_on`,
  build-arg wiring, all asserted programmatically). **Flagged as the top
  carry-over item** — an actual `docker compose up --build` run is still
  unverified.
- Ran a full HTTP-level end-to-end replay (`/home/claude/scratch/e2e_check.py`)
  against a live `uvicorn` process, covering every one of the 27 API
  endpoints in the real demo sequence, including fetching the generated
  PDF/DOCX back and verifying their magic bytes (not just a 200 status) and
  an RBAC check (supervisor 403 / admin 200 on rule editing). All passed.
- Cleaned dev artifacts (test DB, uploaded test files, `__pycache__`) and
  added a root `.gitignore` before packaging.
- Wrote the full root `README.md` (all 22 requested sections), including an
  honest Testing section distinguishing real, run-and-passed verification
  scripts from a formal checked-in `pytest` suite (which does not exist
  yet — see Limitations and `TODO.md`'s carry-over list).
- Updated `TODO.md`: Phases 1-4 all substantially complete; two carry-overs
  explicitly flagged for next session (formal pytest suite; an actual
  Docker-daemon-verified build).
- Zipped the project and delivered it with the final 7-point summary the
  PRD requested.

## Session 3 — 2026-08-30
Instruction: continue with the Next.js frontend, shadcn/ui components, and
Docker configuration. Built the entire frontend application (not yet
compiled/verified — that happened in Session 4):
- Full Next.js 14 App Router scaffold, Tailwind config with the exact
  design tokens recorded in `CLAUDE.md`, font loading via a runtime
  `<link>` tag (deliberately not `next/font/google`, to keep `next build`
  usable in network-restricted build environments).
- `lib/`: `types.ts` (mirrors backend Pydantic schemas), `api.ts` (typed
  client for all 27 endpoints), `auth.tsx`, `utils.ts`.
- Found and fixed a real backend bug while building the frontend: two
  Pydantic schemas (`EvidenceOut.url`, `ReportOut.url`) used a plain
  `@property`, which Pydantic v2 silently does not serialize — switched to
  `@computed_field` and confirmed the fix with a direct `model_dump()` check.
- 13-component hand-rolled shadcn-style UI kit; common components including
  the signature `EvidenceChain` square-node stepper motif.
- All pages: login, dashboard, new-inspection wizard (Load Hackathon Demo +
  manual steps), inspection detail (analysis/review/reports/finalize),
  repository, admin rule pack, and started audit trail (finished Session 4).

## Session 2 — 2026-08-29
Triggered by: "Read CLAUDE.md, PRD.md, TODO.md, and CHANGELOG.md... continue
building exactly from where the project currently stands... update TODO.md
and CHANGELOG.md before stopping." Those four files did not exist yet at the
start of this session (verified by listing the repo root) — Session 1 had
gone straight from planning into code. Created all four, seeded from actual
current state, then built out the entire rest of the backend:

- All remaining services: `deps.py`, `storage.py`, `audit.py`,
  `view_models.py`, `pipeline.py`, the full `ocr/` and `extraction/`
  packages, the `rules/` engine + checks, the `reports/` PDF+DOCX
  generators, the `demo/` asset generator, `seed_data/rule_pack_v1.json`
  (11 rules) and `demo_scenarios.py` (3 scenarios).
- All 12 routers + `main.py` wiring (confirmed via `app.openapi()`: 27
  endpoints, all present as designed).
- `seed.py`, full Alembic setup, migration autogenerated + applied.
- Verified the rule engine end-to-end against a standalone script (zero
  DB) for all 3 scenarios before touching routers — exact expected results.
  Later, running the real seed script against a live SQLite DB surfaced a
  genuine bug (a stale in-memory SQLAlchemy relationship collection that
  only manifested when OCR+extraction and compliance-run were chained in
  one long session) — found, fixed, and re-verified.
- Confirmed real Tesseract OCR is actually available in this sandbox and
  works correctly against the generated demo label images (92.4%
  confidence, fully readable text) — not just the mock canned-lookup path.

## Session 1 — 2026-08-29
Received the full MetraCheck brief in chat (the referenced
`MetraCheck_PRD_for_Claude.md` attachment did not actually exist — verified
by listing `/mnt/user-data/uploads`; the brief text itself, plus the
uploaded `README.md` describing a prior browser-only prototype, was
everything provided). Reviewed `frontend-design`, `docx`, and `pdf` skill
guides before starting any file creation.

Built:
- Monorepo scaffold: `backend/` (FastAPI/SQLAlchemy/Alembic) and
  `frontend/` (Next.js) directory trees.
- `backend/requirements.txt`, `backend/.env.example`.
- `backend/app/config.py` — pydantic-settings, SQLite-by-default /
  Postgres-via-env, OCR provider + confidence-threshold settings.
- `backend/app/database.py` — SQLAlchemy engine/session, SQLite/Postgres
  agnostic.
- `backend/app/security.py` — bcrypt password hashing, PyJWT issuance.
- `backend/app/models.py` — full ORM schema: `Role`, `User`, `Product`,
  `Inspection`, `Evidence`, `OCRResult`, `Declaration`, `RulePack`, `Rule`,
  `ComplianceRun`, `Finding`, `OfficerDecision`, `Report`, `AuditEvent`,
  plus all supporting enums.

Key decisions made and recorded in `CLAUDE.md`: rules as versioned DB rows
evaluated by a fixed set of check functions (never hardcoded in the UI);
`counts_toward_score` flag so the readability rule can always advise without
blocking a clean PASS; a 4th finding status `NOT_APPLICABLE`; a
confidence-downgrade mechanic (OCR confidence below threshold turns a would-
be PASS into REVIEW); condition schema that can key off either product
attributes or already-extracted declaration values; exact content/expected
outcome for all 3 demo scenarios; "Rs." instead of "₹" everywhere (ReportLab
base fonts lack the glyph — caught via the `pdf` skill guide before writing
any report code).

Nothing runnable yet at the end of this session — no schemas, services,
routers, seed data, or tests. Continued in Session 2.
