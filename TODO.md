# TODO — MetraCheck

Living checklist. Check items as they're actually done and verified, not
when they're merely written. Organized by phase; phases are mostly
sequential but not strictly.

## Phase 0 — Planning & scaffolding
- [x] Read/absorb the PRD (now saved at `PRD.md`)
- [x] Decide tech stack + architecture (recorded in `CLAUDE.md`)
- [x] Check `frontend-design`, `docx`, `pdf` skill guides
- [x] Create monorepo directory scaffold (`backend/`, `frontend/`)
- [x] Establish `CLAUDE.md` / `PRD.md` / `TODO.md` / `CHANGELOG.md`

## Phase 1 — Backend foundation
- [x] `requirements.txt`, `.env.example`
- [x] `app/config.py`, `app/database.py`, `app/security.py`
- [x] `app/models.py` — full SQLAlchemy schema (14 tables incl. `Role`)
- [x] `app/schemas.py` — Pydantic v2 request/response models
- [x] `app/deps.py` — `get_current_user`, `require_roles`
- [x] `app/services/storage.py` — local storage backend (S3-shaped interface)
- [x] `app/services/ocr/` — `base.py`, `mock_provider.py`, `tesseract_provider.py`, `factory.py`
- [x] `app/services/extraction/` — `base.py`, `regex_extractor.py`
- [x] `app/services/rules/` — `checks.py`, `engine.py` — verified against all 3 demo scenarios with a standalone script (zero mismatches)
- [x] `app/services/reports/` — `context.py`, `pdf_report.py`, `docx_report.py`
- [x] `app/services/demo/assets_generator.py` — PIL-generated demo label images
- [x] `app/services/audit.py` — audit-log helper
- [x] `app/services/view_models.py` — ORM→schema mapping (not originally itemized; needed once routers started)
- [x] `app/services/pipeline.py` — shared OCR+extraction / compliance-run / report-generation orchestration used by BOTH routers and seed.py (not originally itemized; added so seed data and live use share one code path)
- [x] `app/seed_data/rule_pack_v1.json` — 11 rules, versioned
- [x] `app/seed_data/demo_scenarios.py` — 3 demo scenarios (shared by seed + `/api/demo/load`)
- [x] Real dependency install (venv) + exhaustive import-walk check of every module under `app/` (not just syntax) — caught and fixed one relative-import bug (`context.py`) before it reached routers
- [x] `app/routers/` — all 12: auth, inspections, evidence, ocr, declarations, compliance, review, reports, repository, rules, audit, dashboard, demo
- [x] `app/main.py` — FastAPI app wiring, CORS, `/media` static mount, OpenAPI (confirmed via `app.openapi()`: 27 endpoints, all registered as designed)
- [x] `app/seed.py` — idempotent seed script; runs the full real pipeline (OCR→extraction→compliance→review→PDF/DOCX→finalize) for all 3 scenarios, not a shortcut
- [x] Alembic: `alembic.ini`, `alembic/env.py` (wired to `app.config`/`app.models`), initial migration autogenerated against SQLite (14 tables, DB-agnostic DDL) and applied
- [x] Verify: `alembic upgrade head` + `python -m app.seed` run against a real SQLite DB — all 3 scenarios resolved to the exact expected result/score. **Caught and fixed a real bug** here: a stale in-memory SQLAlchemy relationship cache in `process_ocr_and_extraction` that only manifested when OCR+extraction and compliance-run were chained in one long-lived session (seed.py's pattern) — fixed by appending to the relationship collection instead of a raw FK+`db.add()`. Covered by `tests/test_compliance_flow.py`'s intent even though that file isn't written yet (see below).
- [x] Full HTTP-level end-to-end verification: live `uvicorn` process, every one of the 27 endpoints exercised in the real demo sequence (login → dashboard → demo load → OCR → compliance → officer review → PDF+DOCX generation *with the files fetched back and magic-byte-verified* → finalize → repository search → audit trail → RBAC 403/200 check). All passed.
- [ ] **Carry-over:** `tests/` — a formal `pytest` suite (auth, RBAC, OCR mock, extraction, rule engine, imported-product conditionals, review/audit, reports, repository search, one e2e test per demo scenario). The verification above is real and thorough but lives in one-off scripts under `/home/claude/scratch/`, not a checked-in, re-runnable `pytest` suite. Flagged as the top priority for the next session.

## Phase 2 — Frontend
- [x] Scaffold Next.js 14 app (package.json, tsconfig, tailwind config with the exact design tokens from `CLAUDE.md`, next.config, postcss)
- [x] `lib/api.ts` (full typed client, all 27 endpoints), `lib/auth.tsx`, `lib/types.ts` (mirrors backend schemas), `lib/utils.ts`
- [x] `components/ui/*` — 13 hand-rolled shadcn-style components: button, card, badge, input, label, textarea, select, tabs, dialog, progress, table, alert, spinner
- [x] `components/common/*` — status/result/severity badges, `RuleTag` (the mono "signature" tag), `EvidenceChain` (the square-node stepper signature motif)
- [x] Login page + demo credential one-click quick-fill
- [x] Authenticated app shell (navy sidebar + topbar + `PageShell`, client-side route guard)
- [x] Dashboard (stat cards, violations-by-category + trends charts via Recharts, recent inspections/violations, locations, active rule pack)
- [x] New Inspection wizard (Load Hackathon Demo card + manual Steps 1-4, folding OCR+Declarations into one panel matching the backend's combined endpoint)
- [x] Inspection detail page (full info, editable declarations, Analyze Compliance, findings with officer review actions, generate PDF/DOCX with working download links, finalize)
- [x] Repository (search + status/result/category/supply-type filters + results table)
- [x] Admin → Rule Pack view (read-only for everyone, admin-only edit dialog via RBAC-aware UI)
- [x] Audit trail page (action filter)
- [x] Verify: `npm install` (145 packages, clean), `npx tsc --noEmit` (**zero errors, first attempt**, all 40 files), `npm run build` (**succeeded, first attempt** — 9 routes generated; the only warning was Next.js's font-CSS build-time minification failing due to this sandbox's network egress allowlist not including fonts.googleapis.com, which is an artifact of the sandbox, not a code defect, and doesn't affect the shipped app)

## Phase 3 — Integration & Docker
- [x] `backend/Dockerfile` (python:3.11-slim + tesseract-ocr + libgl1/libglib2.0-0 for opencv-python-headless)
- [x] `frontend/Dockerfile` (3-stage: deps/builder/runner; `NEXT_PUBLIC_API_URL` correctly passed as a build ARG, not just a runtime env var, since Next.js inlines `NEXT_PUBLIC_*` at build time)
- [x] `docker-compose.yml` (postgres w/ healthcheck + backend gated on `service_healthy` + frontend) — YAML-parse-validated and key wiring (healthcheck gate, build arg) asserted programmatically
- [x] Backend entrypoint script (`alembic upgrade head` → `python -m app.seed` → `uvicorn`)
- [x] Full end-to-end walkthrough — done as a live HTTP replay against a real running backend (see Phase 1), not simulated
- [ ] **Carry-over:** `docker compose up --build` was never run against an actual Docker daemon — none was available in this sandbox (`docker: not found`). The compose file and both Dockerfiles are structurally sound and every config decision is deliberate (see README's Docker section), but an actual image build has not been empirically confirmed. **This is the single most important thing to verify first in the next session or before relying on this for a live hackathon demo.**

## Phase 4 — Docs & packaging
- [x] Root `README.md` — all 22 requested sections, including an honest Testing section distinguishing "scripts that ran and passed" from "a checked-in pytest suite"
- [x] Zip the project, present README + zip to the user
- [x] Final summary: what's built, structure, how to run, demo creds, what's real vs mocked, production upgrade path

## Carry-over for next session (see Phase 1 / Phase 3 notes above)
1. Write the formal `backend/tests/` pytest suite (highest priority — logic is proven correct live, just not regression-locked yet).
2. Actually run `docker compose up --build` against a real Docker daemon and fix anything that surfaces (untested territory: apt package availability on the exact base images, build cache behavior, volume permissions).
3. Nice-to-have: frontend component tests (Jest/RTL), `output: 'standalone'` for a smaller frontend image, PaddleOCR provider, real calibrated-measurement architecture.
